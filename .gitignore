# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# This project's own effectiveness ledger is local measurement, not source.
# Keep the directory convention working without committing anyone's usage data.
.jevskill/

# Benchmark output is regenerated by `python bench/run.py`; results.json is
# committed on purpose because the README quotes it. Raw run logs are not.
bench/last_run.txt

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Agent worktrees created by the Claude Code Agent tool (isolation: "worktree").
# They are git worktrees of this repo, never source.
.claude/worktrees/

# Synthetic output of `python bench/cu_run.py --dry-run`. A live run is written with
# --out bench/cu_task_results.json and committed on purpose, like results.json.
bench/cu_runs.json
