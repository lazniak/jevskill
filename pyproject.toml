[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "jevskill"
version = "0.10.0"
description = "Teach your coding agent to stop burning context: Jev (System One) decisions via OpenRouter for routing, triage, classification, gating and reducing large data before it reaches your LLM. A/B tested: 99.3% fewer model input tokens with accuracy up, not down."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "lazniak" }]
keywords = ["jev", "typesafe", "decisions", "llm", "agent", "skill", "openrouter", "classification", "routing"]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
  "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = []

[project.optional-dependencies]
fast = ["httpx[http2]>=0.27", "orjson>=3.9"]
dev = ["pytest>=8", "httpx[http2]>=0.27", "orjson>=3.9"]

[project.urls]
Homepage = "https://github.com/lazniak/jevskill"
Issues = "https://github.com/lazniak/jevskill/issues"

[project.scripts]
jevskill = "jevskill.cli:main"

[tool.setuptools.packages.find]
include = ["jevskill*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"